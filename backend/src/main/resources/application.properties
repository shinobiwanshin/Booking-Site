spring.application.name=tickets

# Database Connection (Railway will provide these via environment variables)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5433/postgres}
spring.datasource.username=${DATABASE_USER:postgres}
spring.datasource.password=${DATABASE_PASSWORD:changemeinprod!}

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Keycloak Configuration
spring.security.oauth2.resourceserver.jwt.issuer-uri=${KEYCLOAK_ISSUER_URI:http://localhost:9090/realms/event-ticket-platform}
keycloak.admin.url=${KEYCLOAK_ADMIN_URL:http://localhost:9090}
keycloak.realm=${KEYCLOAK_REALM:event-ticket-platform}
keycloak.admin.username=${KEYCLOAK_ADMIN_USERNAME:admin}
keycloak.admin.password=${KEYCLOAK_ADMIN_PASSWORD:admin}
keycloak.client.id=${KEYCLOAK_CLIENT_ID:event-ticket-client}
keycloak.client.secret=${KEYCLOAK_CLIENT_SECRET:nfCJmmoiCWbHW38m16bujSBIW3ujoJzN}

# SMTP Configuration for Email Sending
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:supersaiyan2k03@gmail.com}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Frontend Configuration
app.frontend.url=${FRONTEND_URL:http://localhost:5173}

# Server Configuration
server.port=${PORT:8080}

# Actuator for health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always